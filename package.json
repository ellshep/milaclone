{
  "name": "canvas-board",
  "version": "1.0.0",
  "description": "A self-hosted visual board / Milanote-style workspace",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1",
    "express": "^4.21.2",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
